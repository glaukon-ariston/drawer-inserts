feat: add parametric stackable box model and export script

- Create parametric OpenSCAD model (`models/box.scad`) with 45° chamfers for support-free printing.
- Add 3D printing guide and export instructions (`GEMINI.md`) specifically for Creality Ender 3.
- Implement PowerShell script (`scripts/export_stl.ps1`) for automated STL generation via OpenSCAD CLI.
